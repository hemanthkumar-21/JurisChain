{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx azle backend",
    "test": "AZLE_REBUILD=true ts-node -P tsconfig.node.json --transpile-only --ignore=false tests/test.ts",
    "pretest": "AZLE_REBUILD=true ts-node -P tsconfig.node.json --transpile-only --ignore=false tests/pretest.ts"
  },
  "dependencies": {
    "azle": "^0.17.1",
    "ts-node": "10.7.0"
  },
  "devDependencies": {
    "typescript": "^5.1.6",
    "rimraf": "^5.0.1"
  }
}
